# دليل التشغيل لمنظومة مسار

هذا الدليل موجّه للتشغيل والصيانة اليومية، لا لتعديل منطق الكود.

---

## 1. متطلبات التشغيل

المتطلبات الأساسية محفوظة في:

```text
requirements.txt
```

وتشمل عادة:

```text
gradio
pandas
openpyxl
matplotlib
arabic-reshaper
python-bidi
pillow
```

تم تثبيت `pillow` صراحة في 4A لدعم الصور والشعارات وتوليد صور الجداول.

---

## 2. متغيرات البيئة والأسرار

يجب عدم وضع الأسرار داخل الكود.

الأسرار المهمة عادة:

| السر | الغرض |
|---|---|
| `HF_TOKEN` | مزامنة GitHub إلى Hugging Face Space |
| `OWNER_SECRET` أو ما يقابله | رمز/Secret مالك النظام حسب إعداد المنظومة |

تُضبط هذه القيم من إعدادات المنصة:

- GitHub Secrets.
- Hugging Face Space Secrets.

---

## 3. CI/CD بعد 4A

بعد 4A أصبحت القاعدة:

```text
لا تتم مزامنة الكود إلى Hugging Face إلا بعد نجاح Masar Safety Check.
```

التسلسل المطلوب:

```text
masar-safety.yml
    ↓ عند النجاح فقط
sync-to-hub.yml
```

هذا يمنع رفع نسخة تفشل في `check_masar_safety.py` إلى Space.

---

## 4. البيانات والنسخ الاحتياطية

تعمل المنظومة على ملفات JSON داخل مجلد البيانات.

أهم المسارات:

| المسار | الغرض |
|---|---|
| `data/` | مجلد بيانات التشغيل |
| `data/schedules/` | ملفات الجداول المرجعية |
| `data/backups/` | النسخ الاحتياطية |
| `data/audit_log.json` | سجل العمليات الحساسة |
| `data/school_config.json` | إعدادات المدرسة والهوية والتشغيل |
| `data/generated_images/` | الصور المولدة |

الحفظ الآمن يعتمد على:

- كتابة ملف مؤقت.
- التحقق من JSON.
- الاستبدال الآمن.
- الاحتفاظ بنسخ احتياطية.

---

## 5. سجل التدقيق audit

سجل التدقيق يحفظ العمليات الحساسة في:

```text
data/audit_log.json
```

تنبيه مهم جدًا: الحقل الذي يحمل اسم النظام هو:

```text
source
```

وليس `source_name`.

اختبار إصلاح الهوية:

1. غيّر اسم النظام أو الهوية.
2. افتح جلسة ثانية أو دورًا مختلفًا.
3. نفّذ عملية تُسجّل audit.
4. افحص `audit_log`.
5. تأكد أن حقل `source` يحمل الاسم الجديد دون إعادة تشغيل.

---

## 6. إعدادات المدرسة وعدد الحصص

إعدادات المدرسة محفوظة في:

```text
data/school_config.json
```

تشمل عادة:

- اسم المدرسة.
- اسم المنظومة.
- الألوان.
- الشعار.
- عدد الحصص.
- أيام الدراسة.
- أيام الإجازة.

تنبيه: `MAX_PERIODS` يُحمّل عند بدء التشغيل. إذا تغيّر عدد الحصص من إعدادات التشغيل فقد تحتاج المنظومة إلى Restart حتى يعمل التغيير في كل المسارات.

---

## 7. ما لا يُنصح بلمسه دون خطة

لا تعدّل عشوائيًا:

```text
clear_all_data
school_data_panel_js
school_data_tab.select
select_tab_js("مركز البيانات")
ربط Gradio outputs
data/*.json يدويًا أثناء تشغيل المنظومة
```

أي تغيير في هذه النقاط يحتاج فحصًا مستقلًا وخطة واضحة.
