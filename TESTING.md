# دليل الاختبار لمنظومة مسار

هذا الدليل يشرح فحص السلامة الحالي، وخطة الاختبار الوظيفي بعد الاستقرار المعماري.

---

## 1. فحص السلامة المعمارية

قبل رفع أي تعديل شغّل:

```bash
python check_masar_safety.py app.py
```

النتيجة المستقرة الحالية:

```text
PASS: 477 | WARN: 0 | FAIL: 0 | INFO: 6
```

المعاني:

| النوع | المعنى |
|---|---|
| `PASS` | فحص ناجح |
| `WARN` | تحذير يحتاج مراجعة |
| `FAIL` | فشل يمنع الرفع |
| `INFO` | توثيق لحالة مقبولة أو مؤجلة |

---

## 2. خطة 3N-functional-smoke-test

بعد إغلاق التفكيك المعماري، لا نبدأ بتفكيك جديد. نختبر التشغيل الفعلي على Hugging Face Spaces.

الترتيب المعتمد:

1. مركز البيانات Fix 13b.
2. إعدادات الهوية + audit من جلسة ثانية.
3. التوزيع والاحتياط.
4. حالات الإعفاء.
5. جدول اليوم.
6. جدول المعلم.
7. الحسابات وتغيير الرموز.
8. التبادل الودي.
9. التصدير والصور.
10. النسخ الاحتياطي وسجل التدقيق.
11. `clear_all_data` على نسخة اختبارية فقط.

---

## 3. معيار الحسم الثلاثي

أهم ثلاثة اختبارات بعد التفكيك:

```text
1. مركز البيانات:
   كل لوحة تفتح من النقرة الأولى.

2. Audit بعد الهوية:
   حقل source في audit_log يظهر اسم النظام الجديد من جلسة ثانية.

3. التوزيع:
   assign_logic_core ينتج بدائل صحيحة ويحدث الواجهة والأرصدة.
```

قاعدة القرار:

- فشل الثلاثة = فتح `3N-fix1` فورًا.
- فشل نقطة أو نقطتين = تحليل السبب أولًا.
- نجاح الثلاثة = متابعة بقية 3N بثقة عالية.

---

## 4. اختبار الهوية + audit

الخطوات:

1. غيّر اسم النظام أو الهوية من مركز البيانات.
2. احفظ الإعدادات.
3. افتح نافذة مختلفة أو ادخل بدور مختلف.
4. نفذ عملية تُسجل audit.
5. افتح `audit_log` أو لوحة السجل.
6. ابحث عن الحقل:

```text
source
```

النتيجة الصحيحة: يظهر الاسم الجديد مباشرة دون Restart.

إذا ظهر الاسم القديم، فهذا يعني أن مشكلة مرجع `SCHOOL_CONFIG` لم تُحل فعليًا.

---

## 5. اختبارات pytest المقترحة لاحقًا

الخطوة القادمة بعد التوثيق ليست توسيع المنظومة، بل إضافة اختبارات صغيرة للـcores.

البداية المقترحة:

```text
tests/test_auth_core.py
tests/test_storage_write.py
tests/test_assign_logic.py
tests/test_exemptions.py
```

أمثلة لما يُختبر:

- رفض PIN خاطئ.
- حفظ JSON آمن.
- عدم تعيين معلم معفى.
- قراءة إعفاء من `teachers_db`.

---

## 6. ما لا يُختبر على الإنتاج مباشرة

لا تختبر `clear_all_data` على بيانات حية.

إذا احتجت اختباره:

- استخدم نسخة اختبارية.
- استخدم بيانات وهمية.
- تأكد أن غير المالك يُرفض.
- تأكد أن عدد المخرجات لا يكسر الواجهة.
